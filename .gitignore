# Ignore everything by default
*

# === Shared config files ===
!MEMORY.md
!SOUL.md
!IDENTITY.md
!USER.md
!.gitignore
!README.md

# === Memory (user-level, indexed) ===
!memory/
!memory/**

# === Skills (user-level only) ===
!skills/
!skills/**

# === llm-wiki knowledge base ===
!wiki-knowledge/
!wiki-knowledge/**

# Machine-specific files — each machine has its own:
# mcp.json (paths differ), mcp-approvals.json (auto), settings.json (auto),
# models.json (auto), project-map.md (per-machine paths), connectors/
