# 同步规则

- 修改 `~/.workbuddy/` 下文件时，必须同步更新 `D:\hxy\github\workbuddy-sync/` 中对应文件
- 需要同步的文件：`mcp.json`、`MEMORY.md`、`models.json`、`settings.json`、`skills/`、`connectors/`、`memory/`
- 同步后一并 commit + push 到 GitHub
